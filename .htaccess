<IfModule mod_rewrite.c>
RewriteEngine On

# 允許跨域請求 (如果需要的話)
Header set Access-Control-Allow-Origin "*"

# 如果是 API 請求，將所有 /api/ 後面的路徑轉發給 api.php
RewriteRule ^api/(.*)$ api.php?endpoint=$1 [QSA,L]
</IfModule>
